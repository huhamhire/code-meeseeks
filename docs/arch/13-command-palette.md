# 13 · 命令面板

## 职责与边界

标题栏内嵌的 VS Code 风命令面板：快捷操作的统一输入入口，并为「没有直接交互入口」的功能提供可发现、键盘可达的归口。

负责：命令的注册与分域归类、按当前界面语言搜索、两级选择交互、命令的即时执行。不负责：命令背后各功能的实现（复用既有设置 / IPC 能力，不另起一套）、斜杠命令解析（chat 自有，见 [05](05-review-workflow.md)，后续可并入同一注册表）。

## 核心设计

- **触发与位置**：输入框常驻标题栏中部（绝对居中浮层，不占流、不挤压两侧）；快捷键 macOS `Cmd+Shift+P` / 其余 `Ctrl+Shift+P` 打开并聚焦（窗口级渲染层监听，**非系统级 globalShortcut**）。PR 标题留在左侧原位（避开右上 Windows 窗控）；标题过长逼近命令框时右缘渐隐、没入浮层下（输入框不透明底遮盖）——渐隐仅在文字真正触达右缘时出现，短标题不显。
- **两级、不返回上级**：顶层为命令列表；选中「容器型」命令（切换语言 / 主题 / 模型）原地替换为其二级选项列表，选项选中即执行。**最多两级**，不支持返回上级——`Esc` 退出整个面板后重进。叶子命令（打开设置 / 关于 / DevTools、开关代理）直接执行。
- **领域与前缀分类**：命令按领域分组，**领域按英文名字典序固定排列**。每条顶层命令带一个领域 `category`，在列表里作为命令名前缀展示并参与搜索——搜领域名即可归类筛出该域全部命令。P1 三域：**PR**（发现分类直达〔待我评审 / 我创建 等，一级、随平台能力〕、分类筛选〔状态：待处理 / 冲突 / 可合并 等，二级〕、切换 PR 列表）、**评审 / Review**（运行自动评审、切换 AutoPilot、切换对话面板）、**设置 / Settings**（切换显示语言 / 主题 / 模型、切换代理、打开设置 / 关于 / DevTools）。
- **双语展示 + 中英检索**：命令文案随界面语言本地化；**非英语界面**下命令附**英文次行**（对齐 VS Code 显示语言），英语界面只显一行。搜索 haystack **恒含中英两套**——始终支持英文检索（即便次行未显）。语言切换后命令清单按新语言重建（以语言为 key，而非依赖 t 引用变化）。
- **上下文相关命令**：部分命令依当前状态裁剪 / 守卫——「运行自动评审」取当前选中 PR，且对同一 PR 做**重入保护**（已在跑则忽略、无选中则忽略），走与 ChatPane 一键评审同一 `agent:run` 通道，运行态经事件 / store 反映。`when` 化的通用启用判定见「扩展」。
- **即时生效复用既有原语**：命令的执行一律复用设置页同一套「即时生效 + 写盘 + 同步前端配置」链路（界面语言走 i18n 运行时切换 + 持久化、主题走外观 store 派生、模型 / 代理走对应 config 写入 IPC），**不另写一套**，保证与设置页行为一致。深链类命令（打开关于 / 模型分区）打开设置面板并定位到指定分区。
- **注册表 + 分域文件**：命令实现按领域拆分到各自文件，一个注册表聚合所有领域；上层只认「构建顶层命令」这一个入口。新增领域 = 加一个领域文件 + 在注册表登记，上层与交互层不动。
- **二级选项惰性求值**：进入某容器命令时才计算其选项，并读当前配置标注「生效项」（打勾）。模型列表末尾固定「添加模型…」入口（无预设时即唯一项），打开设置的模型分区新建。

## 数据 / 接口契约

- **命令上下文（CommandContext）**：当前配置 + 同步前端配置的钩子 + 「打开设置面板（可指定初始分区）」+ 当前语言的翻译函数。各领域命令构建器都接收它。
- **顶层命令（RootCommand）**：`id` / 本地化 `title` / 领域 `category`；二者择一——叶子的 `run`，或容器的 `options`（惰性返回二级选项 + 进入后的输入占位提示）。
- **二级选项（CommandOption）**：`id` / `title` / 是否 `active`（生效项打勾）/ `run`。
- **设置面板深链**：设置面板接受「初始分区」入参；命令面板的「打开关于 / 添加模型」据此定位分区。

## 扩展与注意事项

- **新增领域命令**：新增一个领域文件导出该域的命令构建器，并在注册表登记即可；命令文案在四语言 locale 的 `commandPalette` 命名空间补齐（递归字典序）。
- **斜杠命令归并**：现阶段不纳入（规划未定）。未来可让 chat 斜杠命令与命令面板共用同一注册表（命令模型已含 `when` 类上下文判定的扩展空间），避免两套定义漂移。
- **自由文本输入类命令**（如「按 URL 打开外平台 PR」）为后续规划：二级层可承载一个接受自由文本的输入项，复用现有输入框与派发，本期不实现。
- **快捷键**：用窗口级渲染层监听而非系统级 `globalShortcut`（后者应用未聚焦也触发，非所需）；跨平台修饰键按 macOS `Cmd` / 其余 `Ctrl` 区分。
- **标题栏空间**：命令面板浮层与 PR 标题、品牌名、平台窗控（mac 红绿灯 / Windows overlay）共处 36px 标题栏，新增标题栏元素须遵守拖拽区 / no-drag 划分，且勿放到右上角窗控覆盖区。

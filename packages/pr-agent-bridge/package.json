{
  "name": "@meebox/pr-agent-bridge",
  "version": "0.0.0",
  "private": true,
  "description": "pr-agent integration bridge (embedded runtime / local CLI strategy)",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "lint": "eslint src tests --max-warnings=0"
  },
  "nx": {
    "targets": {
      "typecheck": { "cache": true },
      "test": { "cache": true },
      "lint": { "cache": true }
    }
  },
  "dependencies": {
    "@meebox/shared": "*",
    "tree-kill": "^1.2.2"
  }
}

{
  "name": "@meebox/rules",
  "version": "0.0.0",
  "private": true,
  "description": "本地 rules.dir 下 markdown 规则文件的加载 + 匹配；产物供 ipc/pr-agent 注入 extra_instructions",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "lint": "eslint src tests --max-warnings=0"
  },
  "nx": {
    "targets": {
      "typecheck": { "cache": true },
      "test": { "cache": true },
      "lint": { "cache": true }
    }
  },
  "dependencies": {
    "gray-matter": "^4.0.3"
  },
  "devDependencies": {
    "vitest": "^4.1.0"
  }
}
